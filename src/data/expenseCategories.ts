import { generatedExpenseCategories } from "@/data/_gen/expenses";
import type { ExpenseCategory } from "@/types/finance";

export const expenseCategories: ExpenseCategory[] = generatedExpenseCategories;
